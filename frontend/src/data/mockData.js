// All mock data has been removed to ensure the application only uses live Firebase data.
// Exports are kept as empty structures to prevent breaking any legacy imports.

export const mockWorkers = [];
export const attendanceData = [];
export const taskDistribution = [];
export const alerts = [];
export const recentActivity = [];
export const stats = {
  totalWorkers: 0,
  activeToday: 0,
  tasksCompleted: 0,
  alertsOpen: 0,
  civicTrustScore: 0
};
export const weeklyPerformance = [];
export const mockMissions = [];
export const mockCollections = [];
